package Oops;

public class Calc1 {
    int num=5;
    public Calc1(int num)//we received the para Pass by constructer in abc
                         //java Allow the vairable name different but the data type want same as
    {
        this.num=num;   //this keyward is for detect its instance variable
                        //we can take a same name of instance variable and local variable
        System.out.println("hello Constructer");
    }

}
